[project]
name = "sample-python-project"
version = "0.1.0"
description = "Fixture: FastAPI sample project for indexer tests"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
